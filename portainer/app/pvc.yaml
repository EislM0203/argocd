---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portainer
  annotations:
    numberOfReplicas: '1'
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 25Gi
  storageClassName: longhorn-storage
status: {}